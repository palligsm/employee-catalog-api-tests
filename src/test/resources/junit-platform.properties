# Display method @DisplayName values nicely and keep execution deterministic.
junit.jupiter.testinstance.lifecycle.default=per_class
junit.jupiter.displayname.generator.default=org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores

# Run test *classes* sequentially (each class talks to a shared remote server),
# but this can be flipped on if the host proves stable enough.
junit.jupiter.execution.parallel.enabled=false
